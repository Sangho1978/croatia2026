# MIX02 검증 보고서

- 원본 ZIP SHA-256: `96f9f40410fbeff3a504da016802cf60486c2142953197375cc9d3b88b66303e`
- 전체 수정 기준: MIX01 원본 복사 후 출석과 준비목록만 교체.
- 보존된 HTML 섹션: 13개, 직렬화 비교 모두 동일.
- 설정/명부/일정/공동경비/사진/지도 라우터/화장실/기존 CSS/Firebase Rules/이미지 보존 검사: 모두 동일.
- 344×882, 390×844, 690×960, 884×1100에서 확인.
- 테스트 85개 통과. 테스트 중 JavaScript page error 0건.
- 두 독립 브라우저의 한상호 / 변태윤 로그인, 시작, 본인 체크, 2/28 반영, 리셋, 새 회차, 이전 회차 지연 이벤트, 체크 중복, 오프라인, 쓰기 실패, 전체 28 완료를 검증.
- 실시간 스트림 처리, PATCH 처리, 스트림 장애 때 5초 조회, 재연결 복귀를 모의 서버로 검증.
- 조장 첫 위치 유지, 교수 포함 28명, 큰 글씨에서 가로 넘침 없음.
- 준비물 50개, 기존 체크 일부 이관, 저장/재접속 유지, 미준비 필터/초기화 검증.
- 사람/화장실/추천앱/일정/위치/호텔/가이드/영상/연수/사진/경비 메뉴와 기존 접근 제한 검증.
- 외부 실서버를 호출하거나 데이터·Rules를 변경하지 않았습니다. 테스트 HTTP 응답과 EventSource 이벤트는 모의 데이터입니다.

세부 체크 목록은 `VERIFY_MIX02.json`에 있습니다. 스크린샷에 나타나는 출석 인원·시간은 테스트용 예시이지 실제 여행 출석기록이 아닙니다.

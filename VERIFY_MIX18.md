# MIX18 검증 요약

- 공동경비 장부는 `date`(사용일) 기준으로 필터·정렬·날짜 그룹을 생성합니다.
- 2026-07-26처럼 여행기간 밖 사용일도 날짜 탭과 해당 날짜 그룹에 나타납니다.
- 각 건에 등록(저장) 시각과 마지막 수정 시각을 현지/한국 기준으로 표시합니다.
- 한상호·이상미에게 `삭제` 버튼을 표시합니다. 삭제는 soft-delete이며 합계/조회/CSV/Excel에서 제외됩니다.
- Excel 정산파일은 외부 CDN 없이 브라우저에서 `.xlsx`를 생성하며 4개 시트(경비내역, 참여자별상세, 참여자별정산, 참여자명단)를 포함합니다.
- `xlsx-lite.js`가 생성한 테스트 XLSX를 openpyxl로 열어 시트·한글·수식·cached value를 확인했습니다.
- JavaScript 문법 검사와 Firebase Rules JSON 문법 검사를 통과했습니다.

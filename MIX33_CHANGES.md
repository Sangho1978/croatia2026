# MIX33 통합본

- MIX32A 현장 운영형 + MIX32B 카드형을 하나의 앱으로 통합
- 상단 오른쪽 아이콘 버튼으로 즉시 전환, 선택은 기기에 저장
- 두 UI는 동일 DOM/동일 Firebase/동일 기능을 공유하므로 기능 차이 없음
- 카드형 관광가이드는 큰 사진 + 넓은 설명 + 1열 명소 카드로 재구성
- 카드형 첫 화면 히어로 이미지는 여행 날짜에 맞춰 로컬 이미지로 변경
- 해외 데이터 절감: 같은 출처 정적 파일 cache-first, 이미지 lazy/low priority, 지도는 펼칠 때만 로드
- 날씨 응답 1시간 로컬 캐시, 환율 네트워크 갱신 1시간, 뉴스는 뉴스 화면을 열 때 갱신
- 안내소책자 PDF는 사전 다운로드하지 않고 첫 보기/다운로드 때만 약 11MB 1회 저장, 이후 Cache Storage 저장본 우선
- 브라우저 보안상 휴대폰의 일반 Downloads 폴더를 앱이 검색하지는 못하며 앱 전용 로컬 캐시를 사용
- 기존 Firebase Rules/데이터 구조 변경 없음

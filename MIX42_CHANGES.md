# MIX42 LOGIN + HOTEL FIX

- 루트 `index.html`을 공통 로그인 게이트웨이로 변경했습니다. 이름+PIN으로 크로아티아/튀르키예1팀을 판별한 뒤 해당 데이터팩으로 자동 이동합니다.
- `platform/login-directory.js`를 index 로그인에도 직접 로드합니다.
- 곽영은 7895, 김수정 8112, 정호용 5619 경로를 재검증했습니다.
- 한글 이름 NFC 정규화, 공백 및 제로폭 문자 제거를 적용했습니다.
- Firebase Rules는 앱 로그인 자체와 무관합니다.
- 크로아티아 호텔 이미지 5장을 서비스워커 필수 캐시에 추가했습니다. 오프라인/불안정 네트워크에서도 호텔 사진이 유지됩니다.
- 호텔 이미지 URL을 MIX42로 버전 갱신해 오래된 브라우저 이미지 캐시 영향을 줄였습니다.
- 서비스워커 캐시를 `gspa-static-v42`로 올려 MIX41 캐시를 자동 정리합니다.

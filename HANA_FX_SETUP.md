# 하나은행 EUR 매매기준율 연결 (MIX11)

앱 상단 환율은 이제 ECB/일반 시장환율이 아니라 **하나은행 EUR 매매기준율만 표시**합니다.

## 기본 동작
1. `js/config.js`의 `HANA_FX_ENDPOINT`가 설정되어 있으면 그 JSON endpoint를 최우선 사용합니다.
2. 설정이 없으면 공개 환율 페이지에 표시된 `하나은행 고시회차`의 EUR 값을 중계 경로로 확인합니다.
3. 네트워크/CORS 실패 시 `data/hana-eur.json`의 마지막 검증 스냅샷 또는 브라우저에 마지막으로 정상 저장된 하나은행 값을 표시합니다.
4. 브라우저가 화면에 보이는 동안 15분 간격으로 새 값을 확인합니다. 은행 고시가 바뀌지 않으면 값은 그대로입니다.

## 공식 Hana API On 또는 자체 서버를 연결하려면
브라우저 HTML 안에 API 비밀키를 넣지 마세요. 서버/프록시가 아래 JSON을 반환하도록 한 뒤 `js/config.js`에 URL만 넣습니다.

```js
const HANA_FX_ENDPOINT='https://YOUR-SERVER.example/hana/eur';
```

응답 예시:
```json
{
  "currency": "EUR",
  "baseRate": 1593.98,
  "date": "2026-09-18",
  "announcedAt": "21:07",
  "round": 1335
}
```

## 표시
- `€1 = ₩...` : 하나은행 매매기준율
- 기준일/고시시각/고시회차를 함께 표시
- 실제 현찰 환전·카드 청구금액은 은행 스프레드와 결제 시점 때문에 다를 수 있습니다.

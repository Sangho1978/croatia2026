# MIX04 검증 요약

기준 원본: croatia2026_mix03_github_pages.zip

## 수정 범위
index.html의 경비 메뉴 조건과 자산 버전, js/router.js의 경비 접근 조건, js/integration.js의 경비 전용 이름 조건, js/expenses.js의 UID 승인 제거, js/receipt-store.js의 경비 관리자 조건만 변경했습니다.

조편성·직책·로그인·출석·위치·일정·화장실·추천앱·준비물·사진앨범 모듈·CSS·이미지는 MIX03과 동일합니다.

## 실행 검증
- 이상미·한상호 로그인으로 경비 접근 가능; financeManagers 요청 0건.
- 서로 다른 두 브라우저에서 28명 참여 저장, 3명으로 수정, 영수증 저장·조회를 확인.
- 새 브라우저/다른 모의 UID에서도 등록없이 경비 접근 확인.
- 28명 이름 모두 조건 검사: 허용 두 명.
- 위재복·장현웅·나종민·김남구 로그인 화면에서 경비 메뉴 숨김·내부 라우트 제한·클라이언트 API 조건 확인.
- 기존 규칙으로 인한 403 응답시 UID 등록이 아닌 한 번의 Rules 변경 안내.
- 390px/884px의 메뉴·사람·출석·일정·화장실·추천앱 전환, 344px의 경비 접근 확인.

## 한계를 명확히 합니다
브라우저 탐색이 제한된 테스트 환경에서 단일 HTML을 렌더했으며, Firebase REST응답·위치권한·localStorage/sessionStorage·IndexedDB 초안 저장부는 모의 처리했습니다. 사진 압축과 앱 렌더링·입력·요청은 실제 코드로 실행했습니다.

Firebase Rules는 JSON/구조·병합·기존 검증조건 보존을 검사했지만 Firebase Emulator와 실서버의 규칙 엔진에서 실행하지는 않았습니다. 운영 Firebase에 쓰기·규칙 게시는 하지 않았습니다.

이 변경은 이름 기반 **앱 화면 제한**입니다. 실명 보안인증이 아니며, 인증된 클라이언트가 앱을 변조하면 서버 접근을 우회할 수 있습니다. 이 보안 한계는 요청된 간편 모드에 따릅니다.

UI/test assertions: 62 passed. Page errors: 0.

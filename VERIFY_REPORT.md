> MIX01 원본의 과거 검증 보고서입니다. 이번 MIX02 결과는 VERIFY_MIX02.md를 참조하세요.

# MIX01 검증 보고서

## 원본 고정
- 기준: 사용자가 현재 요청에 첨부한 화장실·추천앱 보강판 ZIP
- SHA-256: `790ad4a84cd4d11f2d36ce9a8333cc6acda151b665d3ccdcf64a60d1945abb9b`
- 별도의 오늘 통합판이나 샘플 프로토타입 콘텐츠로 대체하지 않았습니다.

## 보존 대조
- 기존 관광 가이드·호텔·영상·체크리스트·연수·긴급·화장실·추천앱·전체동선·날씨 상세 **10개 섹션의 텍스트가 원본과 동일**합니다.
- `data/itinerary.js`, `data/checklist.js`가 원본과 바이트 단위 동일합니다.
- 14개 이미지 모두 바이트 단위 동일합니다.
- Firebase/Google Maps `js/config.js`가 원본과 동일합니다.
- 변경된 것은 UI 구조/조편성/운영 직책/경비·앨범 기능/공통 API 본문 계측/익명 인증 중복 호출 방지입니다.
- 사람별 기존 위치·출석 슬롯은 28명 모두 유지했습니다.
- 로컬 CSS·JS·이미지 참조 누락: 0.

## UI/데이터 흐름 자동검증
107항목 통과, JavaScript page error 0건.

뷰포트: 344×882 / 390×844 / 690×960 / 884×1100.

검증 내용:
- 로그인, 상단 날씨·데이터 사용량 고정, 하단 또는 좌측 5메뉴 화면 내 배치
- 그룹에 출석/사람 2탭만 표시
- 전체 28명, 각 조 조장 우선, 비조장 발표자 3명, 연수 운영진 배지 4명
- 일정 10/16과 날짜별 화장실 카드, 관광지 인라인 펼침/닫힘
- 화장실 16곳·추천앱 8개·더보기 화면전환
- 운영진 경비권한 확인, 기본 전체 28명 선택
- 경비 저장, 3명 개별 참여자로 수정, 0명 입력 거부
- 다른 운영진 브라우저에서 저장된 인원·명단 조회
- 동시수정 ETag 충돌 감지 및 기존 기록 미덮어쓰기
- 새 페이지에서 로컬 초안 복원
- 일반 조장은 경비 메뉴 비노출 및 직접 경로 진입 차단
- 운영진 이름만 입력하고 UID 승인 없는 경우 공유 저장 차단
- 운영진 앨범 연결, 일반 원우의 앨범 열기 및 관리화면 비노출
- 기존 한상호 출석 시작·본인체크·기존 DB 경로/슬롯 유지

UI 저장 테스트는 모의 HTTP 응답과 공유 메모리 DB로 실행했습니다. **운영 Firebase 데이터에는 쓰지 않았습니다.** 스크린샷의 금액·권한 접속 상태는 테스트 예시이며 실제 사용 내역이 아닙니다.

## Firebase Rules 검사
- 추가 Rules JSON 파싱 성공
- 113개 규칙 표현식의 JavaScript 문법 검사
- 20개 내부 정책 시나리오 통과 (운영진/일반인/위조 이름/인원수/금액/작성자 불변성/변경 버전)
- 이 검사는 Firebase Emulator 또는 실제 서버에서 실행한 검증이 아닙니다. 환경의 네트워크 제한으로 Emulator 도구를 설치하지 못했습니다.
- 실제 도메인에 게시한 후 승인된 운영진과 일반 사용자 기기로 서버 권한을 확인해야 합니다.

## 의도적으로 바꾸지 않은 것
- 여행 요금·운영시간·기관·연락처의 최신성은 이번 요청에서 재조사하지 않았습니다.
- 기존 출석 시작/리셋과 로그인 저장방식은 첨부 원본의 방식입니다.
- 새 경비 서버 권한은 기기 UID 수동 승인으로 제한합니다. 기존 이름+전화 끝자리 로그인을 실명 인증이라고 주장하지 않습니다.

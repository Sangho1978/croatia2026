# MIX37 FLOW FIX

- 일정 화면에서 일부 삼성 브라우저에서 제목과 일정 흐름 사이에 큰 빈 공간이 생기던 문제를 CSS 레이아웃 리셋으로 수정.
- 날짜 요약 카드를 `제목 → 일정 흐름 → 전체 경로 → 상세가이드` 순서로 고정.
- `호텔 조식 → 관광 → 이동 ...` 흐름을 텍스트/화살표가 아니라 번호가 붙은 둥근 pill 버튼형 칩으로 변경.
- 일정 흐름은 좌우 스크롤하지 않고 여러 줄로 자동 줄바꿈하므로 작은 화면에서도 잘리지 않음.
- 상세가이드 링크도 라운딩 버튼형으로 복원.
- 날짜별 지도 카드에 이미 전체 동선 버튼이 있으므로 요약 카드의 중복 Google 전체동선 버튼은 숨김.
- 브라우저 pinch zoom은 계속 허용하며, 가로 overflow는 viewport 안에서 정리.
- Fold/태블릿 2열 레이아웃, 오프라인, Firebase 구조/Rules는 변경 없음.

# MIX17 검증

- 원인: 공동경비 CSS가 `body[data-finance-manager="true"]` 상태를 전제로 했으나 로그인 역할 적용 코드가 이 속성을 설정하지 않아, 한상호·이상미 계정에서도 등록 패널이 숨겨졌음.
- 수정: 로그인/로그아웃 시 `data-finance-manager=true|false`를 명시적으로 동기화하고, CSS도 속성이 명확히 `false`인 경우에만 등록 패널을 숨기도록 변경.
- 390x844 자동 UI 확인:
  - 한상호: `경비 등록` + `날짜별 장부` 표시, 등록 폼/카메라 버튼 표시 PASS
  - 이상미: `경비 등록` + `날짜별 장부` 표시, 등록 폼/카메라 버튼 표시 PASS
  - 김남구: `날짜별 장부`만 표시, 등록 폼 숨김 PASS
- JavaScript 문법 검사 PASS
- 로컬 파일 참조 누락 0
- Firebase Rules/저장 경로 변경 없음
